# build-and-push.sh 에서 사용하는 값들의 예시.
# 실제 값으로 채워서 환경변수로 export 하거나 명령 앞에 붙여 사용한다.

# Docker Hub 네임스페이스(사용자명)
DOCKERHUB_NS=your-dockerhub-username

# 이미지 태그 (취약=1.0, 하드닝=2.0 권장)
TAG=1.0

# 사용할 Dockerfile (취약=Dockerfile, 하드닝=Dockerfile.hardened)
DOCKERFILE=Dockerfile
