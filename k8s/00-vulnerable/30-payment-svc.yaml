apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-svc
  namespace: skillshop
  labels: { app: payment-svc }
spec:
  replicas: 1
  selector:
    matchLabels: { app: payment-svc }
  template:
    metadata:
      labels: { app: payment-svc }
    spec:
      containers:
        - name: payment-svc
          image: skilleat/skillshop-payment-svc:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: skillshop-db, key: DATABASE_URL } }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: payment-svc
  namespace: skillshop
spec:
  selector: { app: payment-svc }
  ports:
    - port: 8080
      targetPort: 8080
